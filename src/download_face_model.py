from ultralytics import YOLO
import os

def download_face_model():
    """Скачиваем специализированную модель для детекции лиц"""
    print("📥 Скачиваем модель для детекции лиц...")
    try:
        model = YOLO('yolov8n-face.pt')
        print("✅ Модель для детекции лиц загружена")
        return model
    except:
        print("❌ Не удалось загрузить модель для лиц, используем стандартную")
        model = YOLO('yolov8n.pt')
        return model

if __name__ == "__main__":
    download_face_model()